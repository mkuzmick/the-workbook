import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["pbs.twimg.com", "files.slack.com", "images.unsplash.com", "assets.aceternity.com"],
  },
};

export default nextConfig;
